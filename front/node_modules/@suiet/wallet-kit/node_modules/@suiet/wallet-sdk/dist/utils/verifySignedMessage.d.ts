import { SuiSignMessageOutput } from "@mysten/wallet-standard";
/**
 * Verify a signed message based on Sui standard
 * @param input
 * @param publicKey
 */
export declare function verifySignedMessage(input: SuiSignMessageOutput, publicKey: Uint8Array): Promise<boolean>;
